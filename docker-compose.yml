name: eddi
services:
  db:
    image: postgres:18
    restart: unless-stopped
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: eddi
    volumes:
      - dbdata:/var/lib/postgresql

volumes:
  dbdata:
